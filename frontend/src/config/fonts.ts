/**
 * Available font stacks, applied as a `font-*` class on <html> by
 * `context/font-provider.tsx`. The matching `--font-*` custom properties live
 * in `src/styles/theme.css`; the webfont <link> lives in `index.html`.
 */
export const fonts = ['inter', 'system'] as const
